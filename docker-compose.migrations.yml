version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=FeedbackKit_Identity
      - FeedbackKit_DB=FeedbackKit_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
